# Build output — regenerated by `npm run compile`
out/

# Dependencies
node_modules/

# Packaged extension
*.vsix

# Agent hook logs. These contain verbatim prompt text and absolute paths from
# this machine, so they must not enter history — git history is not editable
# after the fact without a rewrite.
logs/

# Local agent/editor state
.claude/
.cursor/

# Scratch space
scratch/

# OS noise
.DS_Store
